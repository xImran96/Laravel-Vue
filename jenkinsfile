pipeline {
    agent any
    // agent {'label of agent'}
    stages {
            stage('Composer installation'){
               step{
                sh "echo initializing composer"
                sh "composer install";
                }
            }
            stage('Vue Installation'){
                sh "echo initializing composer"
                sh "npm install"
                sh "npm run dev"
            }
    }
}